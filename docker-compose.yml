services:

  mysql:
    image: mysql:8

    environment:
      MYSQL_ROOT_PASSWORD: sujalroot45
      MYSQL_DATABASE: notes_db

    ports:
      - "3306:3306"

  app:
    build: .

    ports:
      - "8501:8501"

    environment:
      GROQ_API_KEY: ${GROQ_API_KEY}
    depends_on:
      - mysql